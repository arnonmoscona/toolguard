"""Integration layer: the Claude Code wire contract.

A sink -- it imports nothing from toolguard. It describes an external format,
so every other layer may depend on it and it depends on none of them.
"""
