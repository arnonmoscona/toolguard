"""
The TOO-45 verdict-equivalence corpus.

See ``README.md`` in this directory for what the corpus is for, and
``test/unit/test_verdict_corpus.py`` for the replay test that consumes it.

This package deliberately holds no ``test_*.py`` module of its own -- the
replay test lives under ``test/unit/`` per project convention -- so
``unittest discover`` finds nothing to run here.
"""
