# TOO-45 verdict-corpus fixture: pins no_match_fallback = "allow_with_warning".
no_match_fallback = "allow_with_warning"

[permissions]
allow = [
    "Bash(ls *)",
    "Bash(pwd)",
]
deny = [
    "Bash(sudo *)",
]
